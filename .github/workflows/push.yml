name: push
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: convox/ci/docker/build@master
        with:
          image: convox/convox
      - uses: convox/ci/docker/push@master
        with:
          image: convox/convox